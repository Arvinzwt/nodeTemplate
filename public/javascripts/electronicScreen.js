function Clock(node, config = {}) {
    this.config = {
        star_x: config.star_x || 'center',
        star_y: config.star_x || 'center',
        cell: config.star_x || 8,
        long: config.star_x || 2,
        angle: config.star_x || 45,
        gap: config.star_x || 1,
        interval: config.star_x || 10,
    }
}


